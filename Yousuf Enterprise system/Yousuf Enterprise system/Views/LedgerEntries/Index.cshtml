@model IEnumerable<LedgerEntry>
@{
    ViewData["Title"] = "Financial ledgers";
}

<form class="row g-2 mb-3" method="get">
    <div class="col-md-4"><input class="form-control" name="q" value="@ViewBag.Query" placeholder="Ledger entry, party, reference..." /></div>
    <div class="col-auto"><button class="btn btn-outline-secondary">Search</button></div>
    <div class="col-auto"><a class="btn btn-success" asp-action="Create">Add ledger entry</a></div>
</form>

<table class="table table-striped bg-white">
    <thead><tr><th>Ledger #</th><th>Date</th><th>Party</th><th>Type</th><th>Head</th><th>Against Invoice</th><th>Amount</th><th>Status</th></tr></thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.LedgerNumber</td>
            <td>@item.LedgerDate.ToString("dd-MMM-yyyy")</td>
            <td>@item.Party?.FullName</td>
            <td>@item.Type.GetDisplayName()</td>
            <td>@item.Head.GetDisplayName()</td>
            <td>@(item.SalesInvoice?.InvoiceNumber ?? "-")</td>
            <td>@item.Amount.ToString("N2")</td>
            <td>
                @if (item.Type == LedgerEntryType.ContraAdjustment && !item.ApprovedByAdmin)
                {
                    <span class="badge bg-warning text-dark">Pending approval</span>
                    @if (User.IsInRole(AppRoles.SuperAdmin))
                    {
                        <form asp-action="Approve" asp-route-id="@item.Id" method="post" class="d-inline">
                            <button class="btn btn-sm btn-success">Approve</button>
                        </form>
                    }
                }
                else
                {
                    <span class="badge bg-success">Posted</span>
                }
            </td>
        </tr>
    }
    </tbody>
</table>
